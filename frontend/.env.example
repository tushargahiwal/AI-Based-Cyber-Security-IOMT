# Base URL for REST calls (proxied to the FastAPI backend in dev, see vite.config.js)
VITE_API_BASE_URL=/api/v1

# Base URL for WebSocket connections, e.g. ws://localhost:8000
# Leave unset in dev to use the current host (works with the Vite proxy)
VITE_WS_BASE_URL=
